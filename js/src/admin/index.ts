/**
 * Admin entry. Settings + permissions are declared in ./extend (the Flarum 2
 * declarative way); the category / field / button managers ride along as a
 * custom component on the same settings page.
 */
export { default as extend } from './extend';
